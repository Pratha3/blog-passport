<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            color: #fff;
            font-family: 'Arial', sans-serif;
        }
    </style>
</head>

<body class="flex flex-col items-center justify-center p-4">
    <div class="absolute h-full">
        <header
            class="w-full max-w-7xl mx-auto flex justify-between items-center bg-[#1A1A1B] shadow-lg p-6 mb-8 rounded-lg border-b border-gray-700">
            <div class="flex items-center space-x-3">
                <img src="https://www.redditinc.com/assets/images/site/reddit-logo.png" alt="Reddit Logo"
                    class="h-10 w-10">
                <div class="text-3xl font-extrabold text-white"></div>
            </div>
            <nav class="space-x-6">
                <a href="#" class="text-gray-400 hover:text-[#FF4500] transition duration-300">Home</a>
                <a href="#" class="text-gray-400 hover:text-[#FF4500] transition duration-300">About</a>
                <a href="/addUpdateBlog" class="text-gray-400 hover:text-[#FF4500] transition duration-300">Add
                    Blogs</a>
                <a href="#" class="text-gray-400 hover:text-[#FF4500] transition duration-300">Contact</a>
                <a href="/logout" class="text-gray-400 hover:text-[#FF4500] transition duration-300">Logout</a>
            </nav>
        </header>


        <main class="w-full max-w-7xl mx-auto p-6">
            <section class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                <% blogs.map((blog)=> { %>
                    <article
                        class="bg-[#1A1A1B] p-6 rounded-lg shadow-lg border border-gray-600 transform transition duration-500 hover:scale-105 hover:shadow-2xl">
                        <img src="<%= blog.image %>" class="rounded-lg w-full h-48 object-cover mb-4" alt="">
                        <h2 class="text-2xl font-bold text-white mb-3 truncate">
                            <%= blog.title %>
                        </h2>
                        <h4 class="text-lg font-semibold text-orange-500 mb-2 truncate">
                            <%= blog.author %>
                        </h4>
                        <p class="text-gray-400 mb-4 truncate">
                            <%= blog.content %>
                        </p>
                        <div class="flex justify-between">
                            <a href="/addUpdateBlog?id=<%= blog._id %>">
                                <button
                                    class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-400 transition duration-300">Edit
                                    Blog</button>
                            </a>
                            <a href="/deleteBlog?id=<%= blog._id %>">
                                <button
                                    class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-400 transition duration-300">Delete
                                    Blog</button>
                            </a>
                        </div>
                    </article>
                    <% }) %>
            </section>
        </main>




    </div>
</body>

</html>