<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - My Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background-color: #1A1A1B;
            color: #D7DADC;
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            background-color: #29292A;
            border: 1px solid #444;
        }

        .form-input {
            background-color: #3A3A3C;
            color: #D7DADC;
        }

        .form-input:focus {
            border-color: #FF4500;
            box-shadow: 0 0 0 3px rgba(255, 69, 0, 0.5);
        }

        .submit-button {
            background-color: #FF4500;
        }

        .submit-button:hover {
            background-color: #CC3700;
        }

        .underline-link {
            color: #FF4500;
        }

        .underline-link:hover {
            color: #D7DADC;
        }
    </style>
</head>

<body class="flex flex-col items-center justify-center min-h-screen p-4">
    <div class="container w-full max-w-md border p-8 rounded-lg shadow-md">
        <h2 class="text-2xl font-bold mb-6 text-center">Login to My Blog</h2>
        <form action="/login" method="post">
            <div class="mb-4">
                <label for="username" class="block text-gray-400 mb-2">Username</label>
                <input type="text" id="username" name="username"
                    class="form-input w-full px-3 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500"
                    required>
            </div>
            <div class="mb-6">
                <label for="password" class="block text-gray-400 mb-2">Password</label>
                <input type="password" id="password" name="password"
                    class="form-input w-full px-3 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500"
                    required>
            </div>
            <button type="submit"
                class="submit-button w-full py-2 text-white font-bold rounded-lg hover:bg-gray-500 transition duration-200">Login</button>
        </form>

        <p class="mt-4 text-center text-gray-400">
            Don't have an account? <a href="/signup" class="underline-link underline">Sign up</a>
        </p>
    </div>
</body>

</html>